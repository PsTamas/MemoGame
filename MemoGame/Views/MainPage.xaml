<Page
    x:Class="MemoGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MemoGame.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="CardTemplate" x:DataType="local:Card">
            <Rectangle Fill="{x:Bind CardColor}" Width="100" Height="100" />
        </DataTemplate>
    </Page.Resources>

    <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot.RightHeader>
            <AppBarButton Icon="Add" Label="Add Card" Click="{x:Bind CardCollection.AddCard}"/>
        </Pivot.RightHeader>
        <PivotItem>
            <GridView ItemsSource="{x:Bind CardCollection.Cards}" ItemTemplate="{StaticResource CardTemplate}"/>
        </PivotItem>
        
    </Pivot>
</Page>
